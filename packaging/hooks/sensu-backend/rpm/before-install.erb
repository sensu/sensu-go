#!/bin/sh
<%= (common_files || "").split(",").inject("") { |res, f| res + File.read(File.join(prefix, f)) + "\n" } %>

case "$1" in
    1) # initial install
	create_group
	create_user
	;;
    2) # upgrade
	;;
esac
