sudo: required
services:
  - docker

language: go
go:
  - 1.8.x
branches:
  only:
    - master
install:
  - ./build.sh deps
script:
  - ./build.sh
  - ./build.sh docker
after_success:
  - ./codecov.sh -t $CODECOV_TOKEN
